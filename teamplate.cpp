// #pragma GCC optimize("O3,unroll-loops")
// #pragma GCC target("avx2,bmi,bmi2,lzcnt,popcnt")

#include <bits/stdc++.h>

using namespace std;

// Aliases de tipo modernos (substituindo typedef)
using ll = long long;
using ull = unsigned long long;
using pii = pair<int, int>;
using pll = pair<ll, ll>;
using vi = vector<int>;
using vll = vector<ll>;

// Constantes avaliadas em tempo de compilação
constexpr int INF = 0x3f3f3f3f;
constexpr ll LINF = 0x3f3f3f3f3f3f3f3fLL;

// Macros úteis e otimizadas
#define fastio                        \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL);
#define all(x) (x).begin(), (x).end()
#define sz(x) ssize(x)
#define endl '\n'

void solve()
{
    // A lógica do seu problema começa aqui
}

int main()
{
    fastio;

    int t = 1;
    // cin >> t; // Descomente para problemas com múltiplos test cases

    while (t--)
    {
        solve();
    }

    return 0;
}